f=@(x) exp(x);
fname="e^x";


syms x
xx=linspace(-3,3);
for k=1:8
  subplot(4,2,k);
  fk=taylor(exp(x),x,0,'order',k);
  plot(xx,f(xx),xx,fk(xx));
  legend(fname,sprintf("%d. rendű közelítés",k));
  legend('location','northeastoutside');
end
